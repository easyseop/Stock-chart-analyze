"""배포 라이브 스모크체크가 검사하는 '페이지가 살아있다' 마커 — 단일 출처.

daily.yml 라이브 스모크(배포 후 grep)와 tests/test_monitoring.py가 **같은 문자열**을
본다. 템플릿을 바꿔 이 마커가 사라지면 test_monitoring이 CI(=PR)에서 먼저 실패 →
"배포마다 빨강인데 원인 불명"(A7 실측: UI 개편이 grep 마커를 지워 모든 배포가
빨강, 진짜 실패와 구분 불가)을 코드리뷰 단계에서 차단한다.

마커를 의도적으로 바꾸려면 이 파일·템플릿·daily.yml을 함께 갱신하면 된다(테스트 통과).
"""

# 배포된 index.html에 반드시 존재해야 하는 마커(순서 무관).
LIVE_SMOKE_MARKERS = (
    "전환 스크리너",       # <h1> 제목 — 페이지 골격이 렌더됨
    'id="rowsrc"',         # 행 렌더 <template> — 데이터 파이프라인이 살아있음
)
