# 핸드오프 (HANDOFF) — 새 세션 인수인계

> **새 세션에서 시작할 때 이 문서부터 읽으면 맥락이 이어진다.**
> 작업 브랜치: `claude/happy-gauss-cwoq21` (이전: `claude/korean-text-review-o3wmsv`)
> 마지막 갱신: 2026-06-20

---

## 0. 새 세션에서 처음 할 일
1. 이 브랜치 체크아웃: `git checkout claude/korean-text-review-o3wmsv`
2. 이 문서(`docs/HANDOFF.md`)와 `docs/설계결정_v2.md`, `README.md` 읽기
3. (네트워크 열렸으면) `pip install -r requirements.txt && python main.py --csv result.csv` 로 실데이터 확인
4. (네트워크 막혔으면) `python main.py --demo` 로 합성 데이터 동작 확인

---

## 1. 프로젝트 한 줄 요약
차트(OHLCV)만으로 군중 심리(탐욕/공포)를 읽어 **매수·매도·관망 신호 + 손절가**를
종목별 **신호 카드(텍스트/CSV) + 차트(HTML/PNG)** 로 내는 스캐너. 종목 10개(현재 5개 확정).

## 2. 지금까지 만든 것 (완료)
- **v1 코어**: ADX 국면 · MA·다중TF 추세 · RSI · 지지저항/박스/방어선/POC · 거래대금 · ATR 손절·비중
- **점수 체계**: 5개 모듈(trend/rsi/sr/volume/trendline) → 국면 가중치 → **−100~+100 정규화** → 판정
- **코드리뷰 반영**: 10건 수정 (pos_high 오판, 크로스 가드, 리샘플 미완성봉, POC 분산, 라운드넘버, 표시 오도 등) — `git log` 참고
- **v1.5 차트(plotly)**: 캔들+박스권+방어선+POC+이평선+진입/손절/목표 + 마우스오버 용어 툴팁
- **지지/저항 다중기준** (`levels.py`): 스윙피벗 군집(강도점수)·피보나치·라운드넘버·밸류영역(VAH/VAL)
- **추세선** (`trendlines.py`): 하락/상승 추세선 자동검출, 상태(지속·임박·돌파), **하락추세 veto**,
  선의 현재 가격값(`trend_line`)·거리(`trend_dist_pct`)를 데이터로 노출
- **매물대 심리** (`supply.py`): 기간분리 평단(단기 3M/장기 1Y) + 미실현손익 추정(추정평단·손익%·머리위물량)
- 용어사전(`glossary.py`), `--demo`(오프라인 합성데이터), 5번째 데모 시나리오 `reversal`

## 3. 핵심 설계 결정 (요약 — 상세는 docs/설계결정_v2.md)
- 점수 기여 모듈 5개. ADX=게이트, ATR=리스크 계산용(점수 제외)
- 국면별 가중치 매트릭스 (`config.REGIME_WEIGHTS`)
- 시간프레임 = 월·주·일 (년봉 제외: 데이터 부족)
- 판정 임계값: |점수|≥50 적극, ≥20 관심 (`config.VERDICT_STRONG/WEAK`)
- **하락추세 veto**: 하락추세 지속이면 매수 신호 차단 (`config.DOWNTREND_VETO`, 사용자 원칙)
- 매물대 심리는 **점수 미반영**(지지저항과 중복 방지) — 정보 게이지로만 제공

## 4. 종목 (config.STOCKS) — 5개 확정 + 5개 공란
RDDT(레딧) · ENPH(엔페이즈) · HMC(혼다 ADR) · UPST(업스타트) · 006800(미래에셋증권)

## 5. 실데이터 결과 해석 (2026-06-20 재검증 완료 — 5종목 전부 정상)
- 🔴 회피(하락추세 지속): **UPST**(평균 −33%·머리위 77%, 선 −4.2%), **HMC**(−8%·71%, 선 −2.7%)
- 🟢 전환 포착: **ENPH**(하락추세선 **상향 돌파**·머리위 9%, +33점), **006800**(하락추세선 임박 −2.0%+RSI, +33점)
- ⚪ 관망: **RDDT**(전 모듈 중립, 0점)
- 참고: 006800(미래에셋) 4.8만원대(48,750원)는 **정상** — 2025 한국 증권주 랠리 반영(1년새 2만→7만→4.8만 눌림)
- 산출물: `result.csv`(5종목) · `charts/{종목}.html`(5개) 정상 생성 확인

### ⚠️ 한국(KRX) 데이터 소스 수정 (중요)
- 이 환경의 egress 정책은 **naver 차단**(`fchart.stock.naver.com` 403 "Blocked by egress policy"),
  KRX 가격 API는 **안티봇으로 400 "LOGOUT"** 반환 → fdr 기본/KRX 소스로는 한국주 수집 불가였음.
- **해결**: `scanner/data.py`에서 6자리 한국 코드는 **야후 파이낸스 티커**(`YAHOO:{code}.KS`→없으면 `.KQ`)로 수집.
  야후는 미국주와 동일 소스라 egress 허용 범위 안 → 가장 안정적. (006800 = `006800.KS`, 1830행 정상)

## 6. 알아둘 한계 / 주의
- **이전 원격 환경은 네트워크 차단**으로 실데이터 불가였음 → 새 환경은 네트워크 허용 정책 필요(§아래)
- AI는 정확한 실시간 시세를 모름 → **이상치는 데이터 내부 일관성으로만 검증**(거래량0·가격 급점프·음수 등),
  AI 기억과 비교 금지
- `ta` 라이브러리 미사용(빌드 실패) → ADX/RSI/ATR은 pandas 직접 구현
- 차트가 선이 많아 다소 빽빽함 → **해결: 대시보드 토글 버튼**(`scanner/dashboard.py`)

## 6.5 백테스트 핵심 결론 (사용자 방향: 차트 전용 '리스크 관리' 도구)
- **방향 예측 엣지는 사실상 0** — 무차별 매매 시 승률 ~33%(1:2 손익분기와 동일), 기대값 ≈ 0.
- **리스크 관리는 작동** — 모든 손실이 약 −1R로 제한(큰 손실 한 방 없음).
- **'전환 후보(하락추세선 돌파)'는 일반 매수보다 낫지 않음**(가짜 돌파 多, −0.07R vs +0.01R).
- **돌파 확인 필터 실험(scanner/backtest.experiment)** — 전환 후보를 양(+)으로 돌리는 법 발견:
  - **거래량≥1.5배**: −0.03R → **+0.05R, PF 1.08, 최대낙폭 −90R→−19R** (가장 효과 큼)
  - 콤보(거래량≥1.3 & RSI<70): +0.04R, DD −30R, 거래수 193(실용적)
  - RSI/돌파폭 단독 필터는 미미. **결론: '거래량 동반' 여부가 진짜 vs 가짜 돌파를 가른다.**
  - ※ 실험은 신호를 '독립 거래'로 측정(신호 품질 비교용). 슬리피지/수수료 미반영.

## 7. 앞으로 할 일 (TODO 후보 — 우선순위 미정)
1. ~~실데이터 재검증~~ ✅ 완료(2026-06-20, KRX 야후 경유 수정 포함)
2. ~~차트 표시 토글~~ ✅ 대시보드 토글 버튼으로 완료
3. ~~간단 백테스트 + 전환후보 검증 + 돌파필터 실험~~ ✅ 완료(`scanner/backtest.py`)
4. **거래량 필터를 라이브 신호/스크리너에 반영** — 전환 후보에 '거래량 동반' 조건 추가(검증됨)
5. **전환 스크리너**: 하락추세선 임박/상향돌파 + 거래량 동반 종목만 자동 솎기
6. **데이터 이상치 자동검증**: 거래량0·가격 급변·결측을 카드에 경고 플래그
7. **머리위물량 점수 반영** 여부 결정(현재 info only)
8. **공란 5종목 채우기** / **자동 실행 스케줄**(장마감 후)
9. **백테스트 고도화**: 추세추종 청산(고정 1:2 대신 추세 이탈까지 보유) 버전 비교, 종목수 확대
10. **v2**: 일목균형표·상대강도·차트패턴

## 8. 실행 명령 빠른참조
```bash
python main.py                       # 5종목 카드(실데이터)
python main.py --csv result.csv      # + CSV
python main.py --chart --png         # + 차트 HTML/PNG (PNG는 kaleido+chrome 필요)
python main.py --dashboard           # 단일 페이지 대시보드(토글·전환후보 분류)
python main.py --dashboard --backtest# + 백테스트 탭(기대값·자산곡선·필터실험) ※ ~4.5분
python main.py --demo                # 네트워크 없이 합성데이터 데모
```

## 9. 네트워크 설정 (새 환경에서 실데이터 받기) — §아래 본문/README 참고
필요 도메인: 미국주 = `query1/2.finance.yahoo.com`, 한국주 = `finance.naver.com`,
`fchart.stock.naver.com`, `data.krx.co.kr`. 또는 전체 허용.
