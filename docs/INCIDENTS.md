# 장애 보고서 — 2026-07-06(월) ~ 07-07(화) 자동매매 첫 실전 주간

> 외부 검토(GPT)용 사실 기록. 시각은 KST. 모든 항목은 로그·워크플로 실행 이력으로 실측 확인됨.

## 배경

- 7/6 낮: 자동매매 신규 규칙(1/3 비중 상한, 트레일링 스탑, 소프트/하드 손절, 재진입 쿨다운, 일일 진입 상한 3, 서킷브레이커 −2%, 어닝 D-1 관리) 배포 완료.
- 7/6 밤 미국장(22:30 개장)이 전체 시스템의 **첫 실전 가동**.

---

## 사건 #1 — GitHub 스케줄러 크론 드랍 (심각도: 높음)

- **증상**: 7/6 16:00~01:20 KST(약 9시간) 동안 예약 빌드·알림봇 크론이 전부 무실행.
  미국장 개장(22:30) 후 1시간 넘게 자동매매가 조용히 정지.
- **탐지 경로**: 시스템이 아니라 **사용자가 "알림이 안 온다"고 물어봐서** 발견 —
  당시 무음이 가장 큰 문제였음.
- **원인(실측 확정)**: ① GitHub Actions `schedule` 이벤트는 best-effort — 고부하 시
  지연·드랍(공식 문서). ② 우리 크론이 정각 피크 분(:00/:15/:30/:45)에 있었음.
  결정적 증거: 같은 시간대에 오프피크 분(7·22·37·52)의 워치독 크론은 발화함.
- **당시 대응**: 커밋 메시지 마커(`[update-top]`)로 푸시 트리거 강제 빌드(수동 킥).
  22:04(빌드)~23:17(매매 판정 완료) 복구. 그 사이 매매 공백 ~47분.
- **영구 조치**: 크론을 오프피크 분(:04/:19/:34/:49)으로 이동 / 워치독 자가치유
  강화(시간당 4회, 정체 시 데이터갱신 재실행) / 푸시 탈출구 상설화.
- **미해결**: GitHub 스케줄러 전체가 죽는 날의 완전 독립 백업(외부 크론+PAT) 미설정.

## 사건 #2 — GitHub Pages 배포 연속 실패 (심각도: 중간)

- **증상**: 7/6 23:17 빌드의 Pages 배포가 "Deployment failed, try again later"로
  2회 연속 실패(37초 간격) → 웹사이트가 낡은 상태로 방치.
- **중요 사실**: 매매·텔레그램 알림은 빌드 단계에서 완결되므로 **매매는 정상 진행**
  — 피해는 대시보드 표시 지연뿐.
- **원인**: GitHub Pages 서비스측 일시 장애(같은 날 스케줄러 불안정과 동반) +
  재시도 간격 37초는 이런 장애가 풀리기에 너무 짧음.
- **조치**: 재시도 전 90초 대기 추가 / 배포 실패 시 텔레그램 자동 알림
  ("매매는 정상, 웹만 지연" 문구로 오해 방지).

## 사건 #3 — 클로드 세션 키퍼 사망 (심각도: 중간)

- **증상**: 백업 스케줄러로 걸어둔 클로드 예약 작업(30분 주기 신선도 점검→자동 킥)이
  세션 비활성 수면과 함께 소멸 — 새벽 00:05~02:44 KST 정체(155분→실측 ~80분)를
  못 잡음.
- **원인**: 플랫폼 제약 — 세션 내 예약 작업은 세션(컨테이너)이 잠들면 사라짐.
  설계 시점에 한계를 인지했으나 실측으로 확인됨.
- **조치**: 보조 수단으로 강등(주 방어는 GitHub 워치독). 완전 독립 백업은
  외부 크론+PAT만 가능하다고 결론.

## 사건 #4 — 워치독 오탐 (심각도: 낮음, 신뢰 비용은 큼)

- **증상**: 7/7 08:52 "100분째 정체" 경보 + 불필요한 재실행. 실제로는 마감후
  풀빌드(07:12)가 정상 완료됐고, 07:12~09:00은 **빌드가 원래 없는 장외 시간**.
- **원인**: 전날 밤 임계를 100분→45분으로 조이면서(포지션 보유 중 공백 최소화 목적)
  장중/장외 구분 없이 적용한 설계 실수.
- **조치**: 장 시간을 아는 임계로 재설계 — 한국장·미국장 중 45분 / 마감후 배치
  구간 150분 / 장외 검사 생략. 9개 시각 케이스 단위검증.

## 사건 #5 — 화면 신선도 오탐 ("155분 전") (심각도: 낮음)

- **증상**: 사용자 폰에서 사이트가 "155분 전 갱신"으로 표시 — 실제 서버는 65분 전
  갱신 상태(당시 기준 정상 범위 근접).
- **원인**: 페이지에 박힌 빌드 시각을 열린 탭의 JS가 계속 카운트업 — 탭을 연 뒤
  서버가 갱신돼도 화면은 모름. "시스템 정체"와 "화면이 낡음"을 사용자가 구분 불가.
- **조치**: 3분마다 서버 HEAD 재조회(Last-Modified)로 실시간 신선도 표시,
  오프라인 시 "이 화면 기준" 라벨.

---

## 종합 — 패턴과 교훈

1. **모든 사건의 공통점: "고장"이 아니라 "무음"이 문제였다.** 매매 로직·계좌·규칙은
   한 번도 틀리지 않았고(위반 0), 인프라가 조용히 멈추거나 화면이 조용히 거짓말했다.
2. 대응 원칙을 "안 죽게"에서 **"죽으면 반드시 알리고(모든 실패 경로 텔레그램),
   스스로 복구(워치독)"**로 전환 — 사건 #1~#5 이후 모든 실패 경로에 알림이 붙음.
3. **남은 구조적 약점**: ① 감시자(워치독)도 같은 GitHub 크론이라 상관 장애
   ② 계좌 상태가 actions/cache 단일 저장(7일 미사용 소멸·임의 evict 가능)
   ③ 경보 채널이 텔레그램 단일 ④ 15분 폴링 + best-effort 스케줄러 조합은
   실계좌 손절 관리에 충분한가 미검증.
